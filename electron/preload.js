// Keep preload minimal and secure; expose APIs here if needed later.
// Currently, we do not expose any Node primitives to the renderer.

// Example: expose read-only app info in future via contextBridge if needed.
// import { contextBridge } from 'electron';
// contextBridge.exposeInMainWorld('cloud-stacker', { version: '1.0.0' });


